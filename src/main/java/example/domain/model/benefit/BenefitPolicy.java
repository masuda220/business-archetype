package example.domain.model.benefit;

import example.domain.model.contract.Contract;
import example.domain.model.fulfillment.FulfillmentHistory;

/**
 * 特典ポリシー
 */
public class BenefitPolicy {
    BenefitItem benefit(Contract contract, FulfillmentHistory fulfillmentHistory) {
        return null;
    }
}
